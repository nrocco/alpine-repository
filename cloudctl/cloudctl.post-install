#!/bin/sh

modprobe tun || true
modprobe tap || true
modprobe virtio-net || true
modprobe nft_reject || true

ip link add cloud type bridge
ip link set cloud up
ip addr add 192.168.2.1/24 broadcast + dev cloud

exit 0
